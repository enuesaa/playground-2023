export const replace = (text: string, prev: string, next: string): string => {
  return text.replace(prev, next)
}

export const count = (text: string): number => {
  return text.length
}

// export const json2yaml = (text: string): string => {
// }
// jsonformat
