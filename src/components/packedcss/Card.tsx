/** @jsxImportSource  @/lib/packedcss */
// import { usePacked } from '@/lib/packedcss'

export const Card = () => {
  // const packed = usePacked()

  // const customCard = packed.card.with({
  //   borderRadius: '5px',
  // })
  
  return (
    <div>
      card
    </div>
  )
}